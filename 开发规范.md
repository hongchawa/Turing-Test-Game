---
name: 项目开发规范
description: PHP、JavaScript、Web 开发统一编码规范
---

# 原则

始终遵循以下原则：

* 优先复用已有代码。
* 保持代码简洁。
* 保持代码一致性。
* 避免重复实现。
* 避免过度设计。
* 不为了"优化"而重写已有代码。
* 修改应尽可能小而精准。
* 优先采用最小改动原则（Minimal Change Principle）。
* 在满足用户需求的前提下，优先选择改动最小、影响范围最小、兼容性最好的方案。

---

# 全局规范

## 修改代码

修改代码前必须：

1. 阅读相关代码。
2. 阅读相关文件。
3. 检查是否已有相同或相似功能。
4. 优先复用已有实现。
5. 仅在确实不存在相关实现时新增代码。
6. 避免复制、粘贴相同逻辑。

如果只是修改已有功能，应直接修改原实现，而不是新增另一份实现。

不要新增类似以下命名：

* xxxNew
* xxxV2
* xxxOptimize
* xxxImproved
* xxxCopy

除非用户明确要求，否则不要：

* 重构整个文件
* 统一代码风格
* 统一命名
* 统一格式
* 替换已有 API
* 修改无关代码
* 移动文件
* 调整项目结构

应仅修改完成当前需求所必须修改的代码。

---

## 代码优化

进行代码优化时：

* 直接修改原有实现即可。
* 不允许新增"优化版""新版""V2"等重复实现。
* 不允许在变量名、函数名、类名、文件名后添加"优化版""优化后的""新版""V2"等后缀。
* 可以在对话中说明进行了优化，但不要在代码中体现。

---

## 注释规范

所有语言必须使用对应语言的标准注释语法。

仅保留真正有价值的注释，例如：

* 解释复杂业务逻辑。
* 说明设计原因。
* 标记需要特别注意的问题。
* 说明特殊实现原因。

不要添加无意义、显而易见的注释，例如：

// 定义变量
const a = 1;

// 获取按钮
const btn = document.querySelector(...);

// 创建对象
const obj = {};

// 添加事件

// 优化后的代码

不要添加：

* 优化后的代码
* 新增代码
* 修改开始
* 修改结束
* TODO（没有具体说明）
* FIXME（没有具体说明）

---

## 保持代码风格一致

修改已有项目时，应保持项目原有风格。

包括但不限于：

* 命名风格
* 缩进风格
* 引号风格
* 注释风格
* 文件组织方式
* 编码习惯

除非用户明确要求，否则不要因为个人偏好统一重构整个文件。

---

# PHP 规范

## PHP 版本

默认以当前稳定版 PHP 为目标。

不要使用：

* 已弃用（Deprecated）的 API
* 已移除（Removed）的 API
* 已弃用的函数参数
* 已弃用的语法

如需兼容旧版本 PHP，仅在用户明确要求时才使用旧写法。

优先参考 PHP 官方文档推荐的最新写法。

---

## PHP GD

编写 GD 图像代码时，应使用当前 PHP 版本支持的 API。

例如：

* `imagedestroy()` → 使用 `unset()`
* `imagefilledpolygon()` → 使用新的三参数写法

GD Image 对象不要调用：`imagedestroy($image);` 应直接：`unset($image); `由 PHP 垃圾回收自动释放资源。

---

## PHP 数值规范

避免依赖 PHP 的隐式类型转换。

对于要求 `int` 的函数、位运算、数组下标、循环次数、字符串重复次数等场景：

不得直接传入 `float`。

应根据业务需求显式使用：

* `(int)`
* `intval()`
* `floor()`
* `ceil()`
* `round()`
* `intdiv()`

避免产生：`Implicit conversion from float to int loses precision` 等警告。

---

# JavaScript 规范

如果当前正在编写 JavaScript：

必须使用 ES6（ECMAScript 2015）及以上语法。

优先使用：

* `const`、`let`，不要使用 `var`
* 箭头函数（适用时）
* 模板字符串
* 解构赋值
* `Promise`
* `async / await`
* `import / export`（模块环境）

优先使用现代 JavaScript 特性。

除非项目已有明确要求，否则不要继续使用已有替代方案的旧语法。

---

# Web 开发规范

## 图标

添加图标时：

优先保持项目已有图标方案。

如果项目已有：

* SVG
* Font Awesome
* Material Icons
* Bootstrap Icons
* 其他图标库

继续保持一致。

如果项目没有任何图标方案：

默认使用内联 SVG。

禁止使用 Emoji 作为界面图标。

---

## 页面设计

页面设计应保持符合项目整体风格。

避免通用 AI 模板风格，包括但不限于：

* Hero + Features + Cards 模板布局
* 紫蓝渐变背景（如 #667eea → #764ba2）
* 大面积玻璃拟态（Glassmorphism）
* 过度使用阴影
* 超大圆角（24px 以上）
* 无意义悬浮动画
* Lorem Ipsum 等占位文本
* 无业务意义的装饰元素

页面设计应优先遵循项目已有设计语言，而不是重新设计整套界面。

---

# 最终要求

完成任何代码修改前，请再次确认：

* 是否复用了已有代码？
* 是否仅修改了必要内容？
* 是否保持了原有代码风格？
* 是否没有引入重复实现？
* 是否没有使用已弃用的 API？
* 是否符合当前 PHP / JavaScript 最新推荐写法？
* 是否没有产生明显的 AI 模板风格？
* 是否真正满足了用户当前需求，而没有修改无关代码？